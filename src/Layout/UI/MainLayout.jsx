import React from "react";

const MainLayout = () => {
  return (
    <div>
      Mainlayout this is used for admin and user those are registerd for app
    </div>
  );
};

export default MainLayout;
